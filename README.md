# Product Binder - Android + iOS
This repository pins the Android and iOS projects at known-good commits so that
contributors can clone one URL and build the whole solution consistently.

- Android submodule: ./android
- iOS submodule:     ./ios

To clone everything: `git clone --recursive https://github.com/<you>/centralrepo.git`
